#!/bin/sh

# create (strict) native windows symlinks from cygwin
export CYGWIN="winsymlinks:nativestrict"

# virtualenwrapper setup
export WORKON_HOME="/.virtualenvs" # a path without space in it is needed (see https://github.com/pypa/virtualenv/issues/53)
