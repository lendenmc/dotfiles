# Agent Instructions
- Never perform or propose git write actions (commit, add, push, amend, rebase, etc.). Only propose commit messages when asked.
- All commit message proposals must follow conventional commits.
- Always check `git diff` before proposing a commit message to ensure it reflects the actual current changes.
