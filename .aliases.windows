#!/bin/sh

if command -v cygstart >/dev/null 2>&1; then
	alias open="cygstart"
fi

# sublime
sublime="/cygdrive/c/Program Files/Sublime Text 3/subl.exe"
# shellcheck disable=SC2139
if [ -r "$sublime" ] && [ -f "$sublime" ]; then
	alias subl="/cygdrive/c/Program\ Files/Sublime\ Text\ 3/subl.exe"
	alias dotop='subl ${DOTFILES}'
	alias dot='dotcd && dotop'
fi
unset sublime
